# Spring Boot 应用配置
spring.application.name=scnai-plant-backend

# 服务器配置
server.port=5000
server.servlet.context-path=/api

# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/scnai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
spring.datasource.username=root
spring.datasource.password=007325
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# 文件上传配置
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB

# JWT配置
jwt.secret=scnai-plant-disease-detection-system-jwt-secret-key-2024
jwt.expiration.remember=604800000
jwt.expiration.normal=86400000

# 文件存储路径
file.upload.path=./uploads/recognition

# AI服务配置
ai.service.url=http://localhost:5001
ai.service.predict.endpoint=/predict
ai.service.enabled=true

# DeepSeek配置
deepseek.api.key=sk-a9139d76dab247c683f8aaff01e011b1
deepseek.api.url=https://api.deepseek.com/v1/chat/completions
deepseek.model=deepseek-chat
deepseek.max.tokens=2000
deepseek.temperature=0.7

# 日志配置
logging.level.root=INFO
logging.level.com.scnai=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n
